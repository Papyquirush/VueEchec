```
Pour lancer l'app :

Back-End : npm install et  pnpm run dev dans le dossier backend
Front-End : npm install et pnpm run dev dans le dossier frontend

Pour revoir une partie -> le frontEnd n'est pas fonctionnel mais la route du back est bien présente (/games/gameStates/{gameId})

profil de user:
{
    "username": "utilisateur1",
    "password": "test"
}
{
    "username": "utilisateur2",
    "password": "test"
}
```